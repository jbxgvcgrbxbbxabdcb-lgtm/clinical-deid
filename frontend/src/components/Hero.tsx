export function Hero() {
  return (
    <section className="hero">
      <h1>
        Detect, <span>review</span>, then redact
      </h1>
      <p>
        Paste text or upload a Word / Markdown note, inspect PHI spans, tick what
        should be redacted, then run de-identification. Nothing leaves your
        machine.
      </p>
      <span className="mock-note">
        请勿上传真实患者数据 / Do not upload real PHI
      </span>
    </section>
  );
}
